# HydroModelMCP HTTP 服务配置

# HTTP 服务器主机地址
# 127.0.0.1 - 仅本地访问
# 0.0.0.0 - 允许外部访问
JULIA_HTTP_HOST=127.0.0.1

# HTTP 服务器端口
JULIA_HTTP_PORT=3000

# CORS 允许的来源（逗号分隔）
# * - 允许所有来源（开发环境）
# 具体域名 - 限制访问来源（生产环境）
# 示例: http://localhost:8080,https://myapp.com
JULIA_HTTP_ALLOWED_ORIGINS=*

# 存储后端配置
# file - 文件存储（默认）
# redis - Redis 存储
STORAGE_BACKEND=file

# 文件存储路径（当 STORAGE_BACKEND=file 时使用）
# HYDRO_STORAGE_PATH=~/.hydro_mcp/storage

# Redis 配置（当 STORAGE_BACKEND=redis 时使用）
# REDIS_HOST=127.0.0.1
# REDIS_PORT=6379

# 存储数据 TTL（秒）
# 默认: 604800 (7天)
HYDRO_STORAGE_TTL=604800
