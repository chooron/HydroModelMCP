interface:
  display_name: "HydroModelMCP Calibration"
  short_description: "Run a full calibration and validation workflow through HydroModelMCP."
  default_prompt: "Use $hydromodel-mcp-calibration to plan or execute a full HydroModelMCP calibration workflow."

policy:
  allow_implicit_invocation: true
